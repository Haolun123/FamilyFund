# 设计方案：定投策略回测

**状态**: 待确认（设计阶段，待决策后实现）
**日期**: 2026-04-17

---

## 一、目标

回答核心问题：**PE×VIX/QVIX 矩阵策略，相比固定金额定投，是否真的更优？**

通过历史数据模拟两种定投策略，对比最终收益、风险指标，验证市场温度计矩阵的有效性。

---

## 二、策略定义

| 策略 | 说明 |
|------|------|
| **固定金额策略** | 每期投入固定基准金额（如 ¥1000），无论市场状态 |
| **PE×VIX 矩阵策略** | 每期投入 = 基准金额 × 当期矩阵倍数，低估多投、高估少投或暂停 |

两种策略使用**相同的基准金额和回测区间**，确保可比性。

---

## 三、支持标的

| 标的 | 价格数据源 | PE 数据源 | 波动率数据源 | 矩阵类型 | 可回测起始 |
|------|------|------|------|------|------|
| 标普500 | yfinance `^GSPC` | ⚠️ 待确认（见第六节） | yfinance `^VIX` | SP500 PE×VIX | ~2005 |
| 纳指100 | yfinance `^NDX` | ⚠️ 待确认（见第六节） | yfinance `^VIX` | NDX100 PE×VIX | ~2005 |
| CSI300 | akshare `sh000300` | akshare `stock_index_pe_lg('沪深300')` | akshare QVIX | CSI300 PE×QVIX | 2015（QVIX 限制）|
| 中证A500 | akshare `sh000510` | akshare `stock_index_pe_lg('中证500')` 代理 | akshare QVIX | A500 PE×QVIX | 2015 |

---

## 四、计算逻辑

### 每期执行流程

```
for each period (月/周):
    1. 取当期指数收盘价
    2. 取当期 PE 值
    3. 取当期 VIX / QVIX 值
    4. 查矩阵 → 倍数（暂停=0x, 观望=?x, 0.3x ~ 顶格）
    5. 固定策略：买入金额 = base_amount
       矩阵策略：买入金额 = base_amount × 倍数
    6. 买入份额 = 买入金额 / 当期价格
    7. 累计份额 += 新份额；累计成本 += 买入金额

期末汇总:
    终值 = 累计份额 × 最新价格
    XIRR = scipy.optimize.brentq(npv_func)
    最大回撤 = 复用 _compute_max_drawdown_series()
```

### "顶格"的处理

矩阵中的"顶格"需要映射到具体倍数。建议：顶格 = 用户自定义上限（默认 10x），或固定为矩阵中最高数值倍数的 1.5 倍。**待确认**。

### "观望"的处理

矩阵中的"观望"是否买入，影响策略收益。**两种选项**：
- **选项 A**：观望 = 0x（不投入）
- **选项 B**：观望 = 0.5x（象征性投入）

**待用户确认**。建议 UI 提供可选项，默认选项 A。

---

## 五、输入参数（UI）

| 参数 | 类型 | 默认值 | 说明 |
|------|------|------|------|
| 标的 | 下拉单选 | 标普500 | 支持 4 个标的 |
| 回测起始日期 | 日期选择 | 2015-01-01 | A股受 QVIX 限制，美股可更早 |
| 定投频率 | 单选 | 月频 | 月频 / 周频 |
| 基准金额 | 数字输入 | ¥1000 | 每期固定策略投入金额 |
| "观望"处理 | 单选 | 投 0x | 投 0x / 投 0.5x |
| "顶格"上限 | 数字输入 | 10x | 顶格对应的实际倍数 |

---

## 六、核心技术难点

### 历史 PE 数据（最大风险点）

yfinance 的 `trailingPE` 仅返回**当前值**，无历史序列。美股历史 PE 方案：

| 方案 | 可行性 | 说明 |
|------|------|------|
| **A. multpl.com 爬取** | 中 | S&P500 月度 PE 有公开数据，但需要爬虫，稳定性差 |
| **B. FRED API** | 高 | 美联储 FRED 有 S&P500 Earnings 数据，可计算 PE，免费 API |
| **C. 用 Shiller CAPE** | 高 | Yale 公开数据，月频，1871 年至今，但 CAPE ≠ 当前 PE |
| **D. 简化：只回测有 akshare PE 的 A 股** | 高（最简单）| 先实现 A 股回测，美股待历史 PE 方案确认后补充 |
| **E. 用固定 PE 阈值区间模拟** | 低 | 失真太大，不建议 |

**建议**：P2 阶段先实现 A 股（CSI300/A500）回测，美股回测列为 P3 等 PE 数据源确认后补充。

**待确认**：用户是否接受方案 D（先做 A 股）？或者有其他美股历史 PE 数据来源？

---

## 七、输出展示

### 对比卡片（核心指标）

| 指标 | 固定策略 | 矩阵策略 | 超额 |
|------|------|------|------|
| 总投入成本 | ¥X | ¥Y | ΔZ |
| 最终市值 | ¥X | ¥Y | ΔZ |
| 绝对盈亏 | ¥X | ¥Y | ΔZ |
| XIRR | X% | Y% | +Z% |
| 最大回撤 | X% | Y% | ΔZ% |
| 定投次数 | N | M（扣除暂停） | — |

### 图表

1. **累计市值对比折线图**：固定策略 vs 矩阵策略的持仓市值走势（两条线 + 累计成本基线）
2. **每期倍数柱状图**：矩阵策略每期买入倍数，直观看到何时加仓/暂停，叠加指数价格走势
3. **资金利用效率**：矩阵策略中"暂停"和"观望"占比饼图

---

## 八、实现文件规划

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/backtest.py` | 新增 | 核心回测引擎：拉历史数据、模拟买入、计算收益指标 |
| `dashboard/app.py` | 修改 | 新增 Tab（回测位于季度财报之后） |

### `src/backtest.py` 核心接口

```python
def run_backtest(
    target: str,               # 'sp500' / 'ndx100' / 'csi300' / 'csi_a500'
    start_date: str,           # 'YYYY-MM-DD'
    base_amount: float,        # 每期基准金额（CNY）
    freq: str,                 # 'M' 月频 / 'W' 周频
    watch_multiplier: float,   # "观望"对应倍数，默认 0.0
    top_multiplier: float,     # "顶格"对应倍数，默认 10.0
) -> dict:
    """
    Returns:
        {
            'fixed':  {'total_cost', 'final_value', 'xirr', 'max_drawdown', 'periods'},
            'matrix': {'total_cost', 'final_value', 'xirr', 'max_drawdown', 'periods'},
            'history': pd.DataFrame,  # 每期明细：date, price, pe, vix, multiplier, cost, value
        }
    """
```

---

## 九、Tab 位置

当前 Tab 顺序：Dashboard / Weekly Update / History / SAP Stock / Market Monitor

规划：
- **Tab 6**：季度财报（P3，待数据迁移）
- **Tab 7**：回测（P2）

如果季度财报长期未开始，可先占 Tab 6 位置，后续插入季度财报时顺移。

---

## 十、待确认事项

1. **美股历史 PE**：接受先只做 A 股回测？还是希望同时支持美股（需要确认 PE 数据源）？
2. **"观望"处理**：实际操作中观望时是否会投入少量资金？建议默认值？
3. **"顶格"映射**：顶格 = 10x 是否合适？
4. **定投频率**：实际你是月频还是周频定投？这影响回测的参考价值
5. **核心关注指标**：最关心 XIRR 超额，还是回撤控制，还是资金效率（少投多赚）？

---

## 十一、依赖

无新增依赖。`yfinance`、`akshare`、`scipy` 均已在 `requirements.txt` 中。
